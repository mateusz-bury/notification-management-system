@* @model IEnumerable<AddUserModel>

@{
    ViewData["Title"] = "Lista użytkowników";
}

<h2 class="text-center">@ViewData["Title"]</h2>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">

            <a asp-controller="AddUser" asp-action="AddUser" class="btn btn-success">Dodaj użytkownika</a>

            <table class="table">
                <thead>

                    <tr>
                        <th>ID</th>
                        <th>Imię</th>
                        <th>Nazwisko</th>
                        <th>Login</th>
                        <th>Email</th>
                        <th>Administrator</th>
                        <th>Opcje</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var user in Model)
                    {
                        <tr>
                            <td>@user.Id</td>
                            <td>@user.Name</td>
                            <td>@user.LastName</td>
                            <td>@user.Login</td>
                            <td>@user.Email</td>
                            <td>@(user.IsAdmin ? "Tak" : "Nie")</td>
                            <td><a asp-controller="AddUser" asp-action="Edit" asp-route-id="@user.Id" class="btn btn-success">Edytuj</a></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
 *@

@model IEnumerable<AddUserModel>

@{
    ViewData["Title"] = "Lista użytkowników";
}

<h2 class="text-center">@ViewData["Title"]</h2>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">

            <a asp-controller="AddUser" asp-action="AddUser" class="btn btn-success mb-3 rounded-pill">Dodaj użytkownika</a>

            <div class="table-responsive">
                <table class="table table-striped table-hover rounded">
                    <thead class="thead-dark">
                        <tr>
                            <th>ID</th>
                            <th>Imię</th>
                            <th>Nazwisko</th>
                            <th>Login</th>
                            <th>Email</th>
                            <th>Administrator</th>
                            <th>Opcje</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model)
                        {
                            <tr>
                                <td>@user.Id</td>
                                <td>@user.Name</td>
                                <td>@user.LastName</td>
                                <td>@user.Login</td>
                                <td>@user.Email</td>
                                <td>@(user.IsAdmin ? "Tak" : "Nie")</td>
                                <td>
                                    <a asp-controller="AddUser" asp-action="Edit" asp-route-id="@user.Id" class="btn btn-primary btn-sm rounded-pill mr-2">Edytuj</a>
                                    @* <button type="button" class="btn btn-danger btn-sm rounded-pill" data-toggle="modal" data-target="#deleteModal@user.Id">
                                        Usuń
                                    </button> *@
                                </td>
                            </tr>

                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
